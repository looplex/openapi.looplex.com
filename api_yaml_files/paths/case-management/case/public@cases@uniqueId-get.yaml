# Linhas com # são comentários e podem ser removidas.
tags:
  - Case Management
summary: "/cases/:case_id"
description: |
  **Retrieve information about a case**
operationId: cases-case_id-get
# Parâmetros da requisição
parameters:
  - $ref: ../../../components/parameters/OcpApimSubscriptionKey.yaml
  - $ref: ../../../components/parameters/case-management/Tenant.yaml
  - $ref: ../../../components/parameters/case-management/BearerToken.yaml
  - name: case_id
    in: path
    required: true
    description: "**Case id**"
    schema:
      type: string
      format: uuid
# Responses
responses:
  # Adicione aqui outras respostas HTTP. Verificar menu "Copiar nova resposta HTTP"
  # Preencha o path do schema de retorno '<response_dto_relative_path_and_name>'
  "200":
    description: "OK"
    content:
      application/json:
        schema:
          $ref: "../../../components/schemas/case-management/CaseEntity.yaml"
  "400":
    description: "Bad Request"
  "500":
    description: "Internal Server Error"