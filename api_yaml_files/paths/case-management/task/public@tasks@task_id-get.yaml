# Linhas com # são comentários e podem ser removidas.
tags:
  - Tasks
summary: Task
description: |
  **Retrieve a task by id**
operationId: tasks-task_id-get
# Parâmetros da requisição
parameters:
  - $ref: ../../../components/parameters/OcpApimSubscriptionKey.yaml
  - $ref: ../../../components/parameters/case-management/Tenant.yaml
  - $ref: ../../../components/parameters/case-management/BearerToken.yaml
  - name: task_id
    in: path
    required: true
    description: "**Task id**"
    schema:
      type: string
      format: uuid
# Responses
responses:
  "200":
    description: "OK"
    content:
      application/json:
        schema:
          $ref: "../../../components/schemas/case-management/TaskEntity.yaml"
  "500":
    description: "Internal Server Error"
    content:
      application/json:
        schema:
          $ref: "../../../components/schemas/responses/InternalServerErrorResponse.yaml"
