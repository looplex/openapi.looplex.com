title: JWT Verify
required:
  - token
  - publicKey
type: object
properties:
  payload:
    type: string
    description: The token.
    example: string
  publicKey:
    type: string
    description: The public key.
    example: string

description: JWT Verify
