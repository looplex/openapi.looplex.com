title: spellcheck
required:
  - text
type: object
properties:
  text:
    type: string
    description: The text to be checked
    example: "Today I saw an acteon move"
  from:
    type: string
    format: (ISO639)
    description: the language of the text of the request
    example: "en-US"
description: Spellcheck