title: Scim
type: object
properties:
  totalResults:
    description: "**Total count of items in the filter**"
    type: integer
    example: 100
  # Resources (implementado com allOf em cada endpoint de query devido à mudança de type do retorno)
  startIndex:
    description: "**Index of search**"
    type: integer
    example: 1
  itemsPerPage:
    description: "**Total count of items to return**"
    type: integer
    example: 12