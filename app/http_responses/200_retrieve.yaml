"200":
    description: "OK"
    content:
      application/json:
        schema:
          # quando endpoint está dentro de uma pasta
          $ref: ../../schemas/<api_folder_name>/{{DTO_NAME}}.yaml

          # quando endpoint está fora de uma pasta
          $ref: ../schemas/<api_folder_name>/{{DTO_NAME}}.yaml 