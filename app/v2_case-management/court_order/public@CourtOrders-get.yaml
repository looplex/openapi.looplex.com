tags:
  - Court Order

summary: "/courtorders"
# summary: "Court_Order"

description: "**Fetch items paginated**"
# description: "**Court_Order**"

operationId: api-v1-CourtOrders-get

# query / path parameters
parameters:
  # quando o endpoint está dentro de uma pasta
  - $ref: ../../parameters/common/OcpApimSubscriptionKey.yaml
  - $ref: ../../parameters/common/Tenant.yaml
  - $ref: ../../parameters/common/BearerToken.yaml
  - $ref: ../../parameters/scim/StartIndex.yaml
  - $ref: ../../parameters/scim/Count.yaml
  - $ref: ../../parameters/scim/Filter.yaml

  # demais parametros


# http responses
responses:
  "200":
    description: "OK"
    content:
      application/json:
        schema:
          allOf:
            # quando endpoint está dentro de uma pasta
            - $ref: ../../schemas/common/ListResponse.yaml

            - type: object
              properties:
                resources:
                  type: array
                  items:
                    # quando endpoint está dentro de uma pasta
                    $ref: ../../schemas/v2_court_order/CourtOrderEntity.yaml
 
  "400":
    description: "Bad Request"
  "401":
    description: "Unauthorized"
  "404":
    description: "Not Found"
  "500":
    description: "Internal Server Error"
    content:
      application/json:
        schema:
          # quando o endpoint está dentro de uma pasta
          $ref: ../../scim/SCIMError.yaml
