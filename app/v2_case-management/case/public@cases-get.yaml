# Linhas com # são comentários e podem ser removidas.
tags:
  - Case Management
summary: "/cases"
description: |
  **Fetch cases paginated**
operationId: cases-get
# Parâmetros da requisição
parameters:
  - $ref: ../../parameters/OcpApimSubscriptionKey.yaml
  - $ref: ../../common_parameters/Tenant.yaml
  - $ref: ../../common_parameters/BearerToken.yaml
  - $ref: ../parameters/StartIndex.yaml
  - $ref: ../parameters/Count.yaml
  - $ref: ../parameters/Filter.yaml
# Responses
responses:
  # Adicione aqui outras respostas HTTP. Verificar menu "Copiar nova resposta HTTP"
  "200":
    description: "OK"
    content:
      application/json:
        schema:
          allOf:
            - $ref: '../../common_schemas/ListResponse.yaml'
            - type: object
              properties:
                Resources:
                  type: array
                  items:
                    $ref: "../schemas/CaseInfoEntity.yaml"
  "500":
    description: "Internal Server Error"
    content:
      application/json:
        schema:
          $ref: '../../scim/SCIMError.yaml'