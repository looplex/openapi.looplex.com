tags:
  - Court Venue
summary: "Paginated list of court venue by level"
operationId: v2.1-court-get
parameters:
  - $ref: ../../parameters/OcpApimSubscriptionKey.yaml
  - name: level
    in: query
    required: true
    description: "**The level of the item lo list**"
    schema:
      type: integer
  - $ref: ../parameters/Page.yaml
  - $ref: ../parameters/PerPage.yaml
  - $ref: ../parameters/ResponseFormat.yaml
responses:
  "200":
    description: "OK"
    content:
      application/json:
        schema:
          oneOf:
            - allOf:
              - $ref: ../schemas/ListResponseLegacy.yaml
              - properties:
                  conteudo:
                    type: array
                    items:
                      $ref: ../schemas/Foro_v2_1.yaml
            - allOf:
              - $ref: ../../schemas/common/ListResponse.yaml
              - properties:
                  resources:
                    type: array
                    items:
                      $ref: ../schemas/Foro_v2_1.yaml