tags:
  - Varas e Foros
summary: "Formatted hierarchy"
description: "Get the formatted hierarchy from this node"
operationId: court-id-formatted_hierarchy-get
parameters:
  - $ref: ../../parameters/OcpApimSubscriptionKey.yaml
  - name: id
    in: path
    required: true
    description: "**The item's identifier**"
    schema:
      type: integer
  - $ref: ../parameters/ResponseFormat.yaml
responses:
  "200":
    description: "OK"
    content:
      application/json:
        schema:
          oneOf:
            - allOf:
              - $ref: ../schemas/ListResponseLegacy.yaml
              - properties:
                  conteudo:
                    type: object
                    properties:
                      hierarquiaCompleta:
                        type: string
                        description: "A hierarquia completa do item"
                      hierarquiaFormatada:
                        type: string
                        description: "A hierarquia formatada do item"
            - allOf:
              - properties:
                  hierarquiaCompleta:
                    type: string
                    description: "A hierarquia completa do item"
                  hierarquiaFormatada:
                    type: string
                    description: "A hierarquia formatada do item"