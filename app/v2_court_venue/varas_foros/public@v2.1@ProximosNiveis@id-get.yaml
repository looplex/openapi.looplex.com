tags:
  - Varas e Foros
summary: "Next levels"
description: "List next levels after this node level"
operationId: varas_foros-proximos_niveis-get
parameters:
  - $ref: ../../parameters/OcpApimSubscriptionKey.yaml
  - name: id
    in: path
    required: true
    description: "**The item's identifier**"
    schema:
      type: integer
  - name: quantidadeNiveis
    in: query
    required: false
    description: "**The level quantity to search** `(default: 2)`"
    schema:
      type: integer
  - $ref: ../parameters/ResponseFormat.yaml
responses:
  "200":
    description: "OK"
    content:
      application/json:
        schema:
          oneOf:
            - allOf:
              - $ref: ../schemas/ListResponseLegacy.yaml
              - properties:
                  conteudo:
                    type: array
                    items:
                      $ref: ../schemas/Foro.yaml
            - allOf:
              - $ref: ../../common_schemas/ListResponse.yaml
              - properties:
                  Resources:
                    type: array
                    items:
                      $ref: ../schemas/Foro.yaml