name: Authorization
in: header
description: |
  **The authorization token**<br/>
  _Bearer \<token_from_authentication_endpoint_response\>_
required: true
example: "Bearer {{token}}"
schema:
  type: string