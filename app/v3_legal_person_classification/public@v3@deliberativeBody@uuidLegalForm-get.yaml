tags:
  - Deliberative Body

summary: "Fetch Deliberative body"

description: "**Fetch Deliberative body through Legal Person Classification unique identifier**"

operationId: v3-deliberativeBody-uuidLegalForms-get

# query / path parameters
parameters:
  - $ref: ../../app/parameters/common/OcpApimSubscriptionKey.yaml
  - name:  uuidLegalForm
    in: path
    required: true
    schema:
      type: string
      format: uuid
      description: "**The legal person classification unique identifier**"

# http responses
responses:
  "200":
    description: "OK"
    content:
      application/json:
        schema:
          type: array
          items:
            $ref: ../schemas/common/ProcedureV3.yaml
  "400":
    description: "Bad Request"
  "401":
    description: "Unauthorized"
  "404":
    description: "Not Found"
  "500":
    description: "Internal Server Error"
    content:
      application/json:
        schema:
          $ref: ../scim/SCIMError.yaml