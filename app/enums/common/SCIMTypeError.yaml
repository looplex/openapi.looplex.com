title: SCIM Type error
type: [string, "null"]
example: invalidFilter
description: |
  **The type of SCIM error if request is GET, otherwise null**<br/>
  invalidFilter<br/>
  tooMany<br/>
  uniqueness<br/>
  mutability<br/>
  invalidSyntax<br/>
  invalidPath<br/>
  noTarget<br/>
  invalidValue<br/>
  invalidVers<br/>
  sensitive