tags:
  - {{TAG_NAME}}

summary: "/{{TAG_NAME}}s/:id"
# summary: {{TITLE}}

description: |
  **Patch {{TAG_NAME}} properties**<br/>
  **If you want to patch CustomFields property see** _<a target="_blank" href="https://www.looplex.com.br/">this link</a>_
# description: {{DESCRIPTION}}

operationId: {{OPERATION_ID}}
parameters:
  - $ref: ../../parameters/OcpApimSubscriptionKey.yaml
  - $ref: ../../{{PATH_PARAMETERS}}/Tenant.yaml
  - $ref: ../../{{PATH_PARAMETERS}}/BearerToken.yaml
  - name: id
    in: path
    required: true
    description: "**The item's unique identifier**"
    schema:
      type: string
      format: uuid
requestBody:
  content:
    application/json:
      schema:
        type: array
        items:
          $ref: ../../common_schemas/PatchEntity.yaml
responses:
  "204":
    description: "No Content"
  {{HTTP_RESPONSES}}
  "400":
    description: "Bad Request"
  "401":
    description: "Unauthorized"
  "404":
    description: "Not Found"
  "500":
    description: "Internal Server Error"
    content:
      application/json:
        schema:
          $ref: '../../scim/SCIMError.yaml'