tags:
  - {{TAG_NAME}}

# escolha um titulo
# summary: "/{{TAG_NAME}}"
# summary: {{TITLE}}

# escolha uma description
# description: "**Fetch items paginated**"
# description: "{{DESCRIPTION}}"

operationId: {{OPERATION_ID}}
parameters:
  - $ref: ../../parameters/OcpApimSubscriptionKey.yaml
  - $ref: ../../{{PATH_PARAMETERS}}/Tenant.yaml
  - $ref: ../../{{PATH_PARAMETERS}}/BearerToken.yaml
  - $ref: ../../common_parameters/StartIndex.yaml
  - $ref: ../../common_parameters/Count.yaml
  - $ref: ../../common_parameters/Filter.yaml
{{PATH_QUERY_PARAMETERS}}
responses:
  {{HTTP_RESPONSES}}