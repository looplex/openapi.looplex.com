tags:
  - {{TAG_NAME}}

summary: "/{{TAG_NAME}}s"
# summary: {{TITLE}}

description: "**Fetch items paginated**"
# description: "{{DESCRIPTION}}"

operationId: {{OPERATION_ID}}

# query / path parameters
parameters:
  # quando o endpoint está dentro de uma pasta
  - $ref: ../../..{{PATH_PARAMETERS}}/OcpApimSubscriptionKey.yaml
  - $ref: ../../..{{PATH_PARAMETERS}}/Tenant.yaml
  - $ref: ../../..{{PATH_PARAMETERS}}/BearerToken.yaml
  - $ref: ../../..{{PATH_PARAMETERS}}/StartIndex.yaml
  - $ref: ../../..{{PATH_PARAMETERS}}/Count.yaml
  - $ref: ../../..{{PATH_PARAMETERS}}/Filter.yaml

  # quando o endpoint está fora de uma pasta
  - $ref: ../../..{{PATH_PARAMETERS}}/OcpApimSubscriptionKey.yaml
  - $ref: ../../..{{PATH_PARAMETERS}}/Tenant.yaml
  - $ref: ../../..{{PATH_PARAMETERS}}/BearerToken.yaml
  - $ref: ../../..{{PATH_PARAMETERS}}/StartIndex.yaml
  - $ref: ../../..{{PATH_PARAMETERS}}/Count.yaml
  - $ref: ../../..{{PATH_PARAMETERS}}/Filter.yaml

  # demais parametros
{{PATH_QUERY_PARAMETERS}}

# http responses
responses:
  {{HTTP_RESPONSES}}
  "400":
    description: "Bad Request"
  "401":
    description: "Unauthorized"
  "404":
    description: "Not Found"
  "500":
    description: "Internal Server Error"
    content:
      application/json:
        schema:
          # quando o endpoint está dentro de uma pasta
          $ref: ../../scim/SCIMError.yaml

          # quando o endpoint está fora de uma pasta
          $ref: ../scim/SCIMError.yaml