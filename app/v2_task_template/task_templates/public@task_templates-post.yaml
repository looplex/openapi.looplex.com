tags:
  - Task Templates

summary: "/TaskTemplates"

description: |
  **Create task templates**<br />
  Item created's guid is returned in **Location header**

operationId: task_templates-post
parameters:
  - $ref: ../../parameters/common/OcpApimSubscriptionKey.yaml
  - $ref: ../../parameters/common/Tenant.yaml
  - $ref: ../../parameters/common/BearerToken.yaml
requestBody:
  content:
    application/json:
      schema:
        $ref: ../../schemas/v2_task_template/TaskTemplateEntityWrite.yaml
responses:
  "201":
    description: "Created"
  "400":
    description: "Bad Request"  
  "401":
    description: "Unauthorized"
  "404":
    description: "Not Found"
  "500":
    description: "Internal Server Error"
    content:
      application/json:
        schema:
          $ref: ../../scim/SCIMError.yaml
