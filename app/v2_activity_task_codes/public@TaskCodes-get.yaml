tags:
  - TaskCodes

summary: "/TaskCodes"

description: "**Fetch items paginated**"

operationId: TaskCodes-get
parameters:
  - $ref: ../parameters/common/OcpApimSubscriptionKey.yaml
  - $ref: ../parameters/common/StartIndex.yaml
  - $ref: ../parameters/common/Count.yaml
  - $ref: ../parameters/common/Filter.yaml  

  - name: Id
    description: "Pode ser passado vários Ids separados por vírgula"
    in: query    
    schema:
      type: string
      format: uuid
  - name: Name
    in: query    
    schema:
      type: string    


responses:
  "200":
    description: "OK"
    content:
      application/json:
        schema:
          allOf:
            - $ref: '../schemas/common/ListResponse.yaml'
            - type: object
              properties:
                resources:
                  type: array
                  items:
                    $ref: ../schemas/v2_activity_task_codes/TaskCodeEntityQuery.yaml  
  "401":
    description: "Unauthorized"
  "404":
    description: "Not Found"
  "500":
    description: "Internal Server Error"
    content:
      application/json:
        schema:
          $ref: '../scim/SCIMError.yaml'